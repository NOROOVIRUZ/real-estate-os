{
  "name": "real-estate-os-worker",
  "version": "1.0.0",
  "description": "Real Estate OS — Cloudflare Worker 백엔드 (Browser Rendering 네이버 호가 + 국토부 실거래 + KV + 프론트 서빙)",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
